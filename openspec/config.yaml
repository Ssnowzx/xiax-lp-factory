schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours

context: |
  Projeto: Xiax LP Factory — landing pages nível estúdio, dark premium e ANIMADAS.
  Stack: Next.js (App Router) + TypeScript + Tailwind + Framer Motion + GSAP/ScrollTrigger + Lenis.
  Referência de motion: atlas.overlens.com.br (GSAP + ScrollTrigger; power3/power4.out).
  Regra: animar só transform/opacity, respeitar prefers-reduced-motion, Lighthouse ≥ 90.
  Skill de referência: .claude/skills/atlas-level-landing/SKILL.md
  Posicionamento: "software e soluções de IA para empresas" (não "feito com IA").
